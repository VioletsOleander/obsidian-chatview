repos:
  - repo: local
    hooks:
      - id: check-format
        name: Check Format
        entry: dprint check --allow-no-files
        language: system
        types_or: [ts, javascript, json, yaml, markdown, svelte]
      - id: check-lint
        name: Check Lint
        entry: npm x eslint
        language: system
        types: [ts]
